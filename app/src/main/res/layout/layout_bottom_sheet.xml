<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="@dimen/lbs_layout_height"
    android:background="@android:color/white"
    android:orientation="vertical"
    app:behavior_peekHeight="@dimen/lbs_behavior_peek_height"
    app:layout_behavior="android.support.design.widget.BottomSheetBehavior">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="@dimen/lbs_padding_text"
        android:text="@string/detected_items"
        android:textSize="@dimen/lbs_text_big" />

    <android.support.v7.widget.RecyclerView
        android:id="@+id/lbs_labels_list"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingEnd="@dimen/lbs_padding"
        android:paddingStart="@dimen/lbs_padding"
        tools:listitem="@layout/row_item" />

</LinearLayout>